name: nextjs_ai_chatbot_mobile
description: A new Flutter project connected to Next.js AI Chatbot
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  google_fonts: ^6.1.0
  intl: ^0.19.0
  flutter_markdown: ^0.7.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
